export type AidType = {
  id: number;
  title: string;
  description: string;
  sum: number;
  link: string;
};

export type AddAidInput = {
  title: string;
  description: string;
  link: string;
};
